# dsh-TUI 0.10.2-async23.3

基于上游 0.10.2，基础提交 `8e1931e7bbd376dc8838d74f2fe24f052012542c`，保留个人版的 Recap 紧凑布局、回到底部按钮开关和 GitHub Release 更新来源。

- 底部原本留白的信息行，在上下文进度条开启且已有用量数据时，默认显示 system / prompt / assistant / thinking / tools 的 token 数及剩余容量 free。
- 鼠标悬停其他状态字段或出现操作提示时，临时显示对应信息；结束后恢复上下文明细。行高保持不变。
- 保留已开启的活动摘要、快捷键提示及极简模式行为。没有用量的新会话不显示虚构的上下文数据；关闭进度条后也不留下默认图例。
- 全屏和 inline 模式均支持默认明细，窄窗口沿用现有的短标签显示。鼠标悬停交互仍由全屏模式提供。
- 同步双语说明，并将上下文明细回归加入发布工作流。

下载 `install.mjs` 后运行 `node install.mjs`。需要 Node.js ^22.19 或 >=24、npm、pnpm 和官方 dsh。安装沿用现有 dsh-tui profile；启动命令为 `dsh-tui` 或 `dst`。已运行的 dsh 需要退出后重新启动。

发布资产：`dsh-tui.tgz`（编译插件）、`install.mjs`（版本固定的安装脚本）、`SHA256SUMS`。DSH 核心仍使用官方运行时，本 Release 不提供独立二进制。

验证：TypeScript 编译和全部 `verify:build` 门禁通过；状态栏 95 项行为断言覆盖全屏/inline、120/80 列、默认显示、悬停切换、提示优先级、恢复和固定行高；问卷、工具卡与会话轨迹布局回归通过。已安装构建的终端帧验证通过，真实 PTY 启动后可正常退出，未发起模型请求。
